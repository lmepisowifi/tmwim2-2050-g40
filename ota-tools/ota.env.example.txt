# ota.env — device-side OTA config. Copy to /lmepisowifi/ota.env on the ONT.
# ota.sh sources this. This file is NOT a release component, so it (and your
# globals.env) survive every update.

# OWNER/REPO of your GitHub repo.
OTA_REPO="YOUR_GH_USERNAME/lmepisowifi"

# Stable URL to the manifest (raw file on your default branch). release.sh
# commits manifest.txt to the repo root, so this URL never changes per release.
OTA_MANIFEST_URL="https://raw.githubusercontent.com/YOUR_GH_USERNAME/lmepisowifi/main/manifest.txt"

# Optional: raw CHANGELOG shown in the admin OTA page.
OTA_CHANGELOG_URL="https://raw.githubusercontent.com/YOUR_GH_USERNAME/lmepisowifi/main/CHANGELOG.md"

OTA_BRANCH="main"

# 1 = cron auto-installs updates; 0 = only notify (admin installs from the UI).
OTA_AUTO="0"

# 1 = also push firmware to the coin-slot NodeMCU during an apply (version-gated).
OTA_NODEMCU="1"

# Optional CA bundle for wget HTTPS verification. If this file is absent, ota.sh
# falls back to --no-check-certificate. Recommended: drop a real cacert.pem here.
# OTA_CACERT="/lmepisowifi/cacert.pem"
